[package]
name = "timbang"
version = "0.1.0"
edition = "2024"

# Tahap 0 only. These get removed together with src/main.rs before Tahap 1 (§9),
# so a throwaway stage cannot decide Tahap 1's runtime for it.
[dependencies]
ureq = { version = "3.3.0", features = ["json"] }
serde_json = "1.0.151"
dotenvy = "0.15.7"
